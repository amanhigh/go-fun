package common

import "github.com/templui/templui/components/badge"

type TypeBadgeProps struct {
	TypeExpr string
	Class    string
}

templ TypeBadge(props TypeBadgeProps) {
	@badge.Badge(badge.Props{
		Variant:    badge.VariantSecondary,
		Class:      "border " + props.Class,
		Attributes: templ.Attributes{"x-bind:class": "presentation.type(" + props.TypeExpr + ").class", "x-text": "presentation.display(presentation.type(" + props.TypeExpr + "))"},
	}) {}
}

type StatusBadgeProps struct {
	StatusExpr string
	Class      string
}

templ StatusBadge(props StatusBadgeProps) {
	@badge.Badge(badge.Props{
		Variant:    badge.VariantOutline,
		Class:      "border " + props.Class,
		Attributes: templ.Attributes{"x-bind:class": "presentation.status(" + props.StatusExpr + ").class", "x-text": "presentation.display(presentation.status(" + props.StatusExpr + "))"},
	}) {}
}
